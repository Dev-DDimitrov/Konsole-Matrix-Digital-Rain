#!/usr/bin/env bash
# SPDX-License-Identifier: GPL-3.0-or-later
# Part of Konsole Matrix Digital Rain.
# User-installed synchronized entry point used by matrix-all and matrix-saver.
set -Eeuo pipefail
exec "$(dirname "$0")/matrix_renderer.py" "$@"
